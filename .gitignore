# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# ── Dati app: DB con segreti (API key) + dati personali — MAI committare ──
deja.db
deja.db-wal
deja.db-shm
*.db-wal
*.db-shm

# ── Build / dist ──
build/
dist/
build_log*.txt
# Redistributable VC++ scaricato a parte per compilare l'installer (vedi deja.iss)
vc_redist.x64.exe

# ── Sito web: ora è in un repo separato e privato (github.com/5cr1p73d/deja-web) ──
/web/

# ── IDE / OS ──
.vs/
.idea/
.vscode/
*.log
.DS_Store

# ── Stato locale editor / tool (può contenere token: NON committare) ──
.claude/
.obsidian/

# ── Vault Obsidian: note interne personali, NON pubblicare ──
obsidian/

# ── File di debug / audio personali ──
*.wav
debug_audio.wav
graphify-out/
design/_rows.png
design/_smoke.png
design/_smoke_expanded.png
design/_smoke_real.png
design/_smoke_explore.png
design/_smoke_shell.png
deja.db*
design/_smoke_settings.png
